# Final Consolidated Changed-File Manifest

## Runtime/build files updated

- `.pages.yml` — simplified labels, dependency descriptions, system visibility and field guidance.
- `assets/js/main.js` — consolidated field normalization, URL safety, visibility synchronization, collision fixes and malformed-entry protection.
- `assets/css/style.css` — collision ownership, responsive fixes, theme/background corrections, wrapping and action alignment.
- `tools/generate-site.js` — raw HTML/SEO/Profile/Contact/Portfolio/Website synchronization and safe links.
- `tools/test-cms-field-coverage.js` — current field/ownership validation.
- `tools/test-deep-cms-behavior.py` — current runtime export contract.
- `tools/test-site-settings-behavior.py` — current runtime export contract.
- `tools/test-website-content-consolidated.py` — consolidated Website content and list regression tests.
- `index.html`, `404.html`, `data/runtime-manifest.json` — generated output synchronized from source JSON.

## Test and evidence files added

The `tools/` and `docs/` folders contain the targeted audit scripts and JSON evidence for each tested CMS group.

## User data

Existing CMS content values were preserved. No content collection was deleted or renamed.
